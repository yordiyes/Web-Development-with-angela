var name = "aBeBE";
var nameSlice = name.slice(0,1)
var restName = name.slice(1,name.length).toLowerCase();
console.log("Hello " +nameSlice.toUpperCase()+restName);
// console.log(name.toLowerCase());